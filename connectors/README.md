# Connectors

Connector directories are adapter implementation homes. MVP connector behavior is manifest-backed and read-only through `packages/family-connectors`.

Do not copy upstream app code into these directories. Add API clients, request schemas, fixture tests, and read-only actions incrementally.

