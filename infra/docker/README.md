# Docker Infrastructure

`docker-compose.yml` currently provides Postgres for local metadata and audit development. Upstream app compose recipes should be added as opt-in profiles after license and security review.

